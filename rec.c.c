// Area of a rectangle
#include <stdio.h>
int main() {
int length,width,mult;
printf("enter length");
scanf("%d",&length);
printf("enter width");
scanf("%d",&width);
mult=length*width;
printf("the area of the rectangle is %d",mult);
}